mod context;
mod api;

fn main() {
    // let mut ctx = Ctx;

    // let client_id = ClientId::new("standalone-light-client", 0).unwrap();

    // // initialize client 
    // let client = ClientState::from();
    // let consensus_state = ConsensusState::from();
    // client.initialise(&mut ctx, &client_id, consensus_state.into());
    // verify client message 
    // update state
    
}
